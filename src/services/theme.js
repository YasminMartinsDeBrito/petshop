import { extendTheme } from "@chakra-ui/react";

const themeFont = extendTheme({
    fonts: {
        heading:`'Roboto', sans-serif`,
        body: `'Roboto', sans-serif`
    }
})

export default themeFont